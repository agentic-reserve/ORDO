{
  "name": "@ordo/api",
  "version": "0.1.0",
  "description": "Ordo REST API Server",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "dev": "bun run server.ts",
    "start": "bun run server.ts",
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "siwe": "^2.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
